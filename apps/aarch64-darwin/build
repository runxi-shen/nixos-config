#!/bin/sh -e

GREEN='\033[1;32m'
YELLOW='\033[1;33m'
RED='\033[1;31m'
NC='\033[0m'

# See ./build-switch for why the host is resolved at runtime.
if [ -n "$1" ] && ! echo "$1" | grep -q '^-'; then
  HOST="$1"
  shift
else
  HOST=$(scutil --get LocalHostName)
fi

FLAKE_SYSTEM="darwinConfigurations.\"${HOST}\".system"

export NIXPKGS_ALLOW_UNFREE=1

echo "${YELLOW}Building for host ${HOST}...${NC}"
nix --extra-experimental-features 'nix-command flakes' build ".#${FLAKE_SYSTEM}" "$@"

echo "${YELLOW}Cleaning up...${NC}"
unlink ./result

echo "${GREEN}Build complete. Nothing was switched.${NC}"
